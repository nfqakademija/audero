{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Response list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Photo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('game_response_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.photo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('game_response_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('game_response_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('game_response_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
